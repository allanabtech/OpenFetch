# Generic HTTP Plugin

This plugin handles downloading direct HTTP and HTTPS URLs.

## Features
- Analyzes URLs using HTTP HEAD request.
- Handles standard HTTP downloads.
- Basic chunked downloading and save support.
